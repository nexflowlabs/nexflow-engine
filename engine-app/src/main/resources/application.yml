nexflow:
  script:
    # Optional: when set, script step can use mode: ISOLATED and delegate to this worker
    # worker-endpoint: http://localhost:9090/execute

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nexflow
    username: nexflow
    password: nexflow

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
